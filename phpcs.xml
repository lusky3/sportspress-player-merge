<?xml version="1.0"?>
<ruleset name="SportsPress Player Merge">
    <description>WordPress Coding Standards for SportsPress Player Merge</description>
    
    <!-- Include WordPress Coding Standards -->
    <rule ref="WordPress"/>
    
    <!-- Check all PHP files -->
    <file>.</file>
    
    <!-- Exclude vendor and node_modules -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    
    <!-- Allow short array syntax -->
    <rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
        <severity>0</severity>
    </rule>

    <!-- Custom capabilities this plugin registers/checks, so current_user_can()
         calls against them don't flag as an unknown capability. -->
    <rule ref="WordPress.WP.Capabilities">
        <properties>
            <property name="custom_capabilities" type="array">
                <element value="edit_sp_players"/>
                <element value="manage_sportspress"/>
                <element value="delete_sp_players"/>
            </property>
        </properties>
    </rule>
</ruleset>